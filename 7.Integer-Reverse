class IntegerRev{
public static int reverse(int x) {
	    int ans=0;
	    int temp=(x>0)?x:(-1)*x;
	    	   	while(temp>0) {
	    	   		ans=(ans*10)+(temp%10);
	    	   		temp=temp/10;
	    	   	}
	    	   return   (x<0)?(-1)*ans:ans;
	 }
}
